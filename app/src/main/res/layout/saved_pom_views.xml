<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/pom_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:textSize="14sp"
        android:textColor="@color/silver">
    </TextView>

    <TextView
        android:id="@+id/pom_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/delete_pom_cycle"
        android:layout_marginEnd="5dp"
        android:textStyle="bold"
        android:textSize="15sp"
        android:layout_marginTop="24dp">
    </TextView>

    <ImageButton
        android:id="@+id/delete_pom_cycle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/pom_view"
        app:layout_constraintTop_toTopOf="@id/pom_view"
        app:layout_constraintBottom_toBottomOf="@id/pom_view"
        android:background="@color/Gray"
        android:layout_marginBottom="4dp"
        android:src="@drawable/delete_24">
    </ImageButton>

    <View
        android:layout_width="match_parent"
        android:layout_height="2dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/pom_view"
        android:layout_marginTop="8dp"
        android:background="@color/black">
    </View>

</androidx.constraintlayout.widget.ConstraintLayout>