<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="40dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_gravity="center"
    android:theme="@style/StatRecyclerViewsTheme">

    <!--Stat views (except activity) constrained from END because width of activity String is variable-->
    <TextView
        android:id="@+id/food_eaten_textView"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="8dp">
    </TextView>

    <TextView
        android:id="@+id/food_portion_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/calories_consumed_textView"
        android:gravity="center"
        android:textSize="17sp"
        android:minWidth="100dp"
        android:maxWidth="150dp">
    </TextView>

    <TextView
        android:id="@+id/calories_consumed_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:gravity="center"
        android:layout_marginEnd="8dp"
        android:textSize="17sp"
        android:minWidth="100dp"
        android:maxWidth="150dp">
    </TextView>

</androidx.constraintlayout.widget.ConstraintLayout>